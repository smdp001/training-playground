pipeline {
    agent { 
		docker { 
			image 'alpine:latest' 
		}
	}
    stages {
		stage('build') {
        	    	steps {
				sh 'date > text.txt'
            		}
        	}
		stage('test') {
            		steps {
				
            		}
        	}
    }
}
