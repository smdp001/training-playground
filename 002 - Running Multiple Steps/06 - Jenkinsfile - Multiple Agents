pipeline {
    agent none
    stages {
    	stage('maven') {
		agent any
		steps {
			sh 'mvn --version'
		}
	}
	stage('npm') {
		agent any
		steps {
			sh 'npm --version'
		}	
	}
    }
}
